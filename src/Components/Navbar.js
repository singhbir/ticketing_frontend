import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar navbar-light" style={{ backgroundColor: "#c0462c" }}>
      <span
        className="navbar-brand mb-0 mx-auto"
        style={{
          fontSize: "30px",
          letterSpacing: "2px",
          color: "#fff",
        }}
      >
        PeerXp
      </span>
    </nav>
  );
};

export default Navbar;
